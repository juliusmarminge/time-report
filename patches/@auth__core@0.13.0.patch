diff --git a/lib/routes/session.js b/lib/routes/session.js
index e496d921578b735e640d1fc766582831dcbfdb1c..483c6e811246a61d82386e2e8c3976c71512820f 100644
--- a/lib/routes/session.js
+++ b/lib/routes/session.js
@@ -91,10 +91,7 @@ export async function session(params) {
             const sessionPayload = await callbacks.session({
                 // By default, only exposes a limited subset of information to the client
                 // as needed for presentation purposes (e.g. "you are logged in as...").
-                session: {
-                    user: { name: user.name, email: user.email, image: user.image },
-                    expires: session.expires.toISOString(),
-                },
+                session,
                 user,
                 newSession,
                 ...(isUpdate ? { trigger: "update" } : {}),